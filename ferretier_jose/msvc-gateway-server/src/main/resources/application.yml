resilience4j:
  circuitbreaker:
    configs:
        default:
            slidingWindowSize: 6 #numero de llamadas que se van a tener en cuenta para calcular el porcentaje de fallos
            slowCallRateThreshold: 50 #porcentaje de llamadas lentas para cerrar el circuito
            waitDurationInOpenState: 20s #timepo en que esta en circuito cerrado
            permittedNumberOfCallsInHalfOpenState: 4 #numero de llamadas que se permiten en estado semi abierto
            slowCallDurationThreshold: 3s #tiempo maximo para considerar una llamada lenta
            failureRateThreshold: 50 #porcentaje de fallos para abrir el circuito
    instances:
        products: #nombre del del circuto
            baseConfig: default #nombre de la configuracion por defecto
  timelimiter:
    configs:
        default:
            timeoutDuration: 20s #tiempo maximo de espera time out y es primero antes que llamada lenta
    instances:
        products:
            baseConfig: default

spring:
  application:
    name: msvc-gateway-server
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:4200"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - Authorization
              - Content-Type
              - Accept
            exposedHeaders:
              - Authorization
            allowCredentials: true
      routes:
          - id: msvc-oauth
            uri: lb://msvc-oauth
            predicates:
              - Path=/api/security/**
            filters:
              - StripPrefix=2


          - id: msvc-users
            uri: lb://msvc-users
            predicates:
              - Path=/api/users/**
            filters:
              - StripPrefix=2


          - id: msvc-persons
            uri: lb://msvc-persons
            predicates:
              - Path=/api/persons/**
            filters:
              - StripPrefix=2

          - id: msvc-catalog
            uri: lb://msvc-catalog
            predicates:
              - Path=/api/catalog/**
            filters:
              - StripPrefix=2

          - id: msvc-info-person
            uri: lb://msvc-info-person
            predicates:
              - Path=/api/info-person/**
            filters:
              - StripPrefix=2
              - TokenRelay


  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9100
      client:
        registration:
          ferr-jose-auth-server:
            provider: spring
            client-id: ferr-jose-auth-server
            client-secret: ldkfjemsDLf@l,dlsdlñldlsd
            authorization-grant-type: authorization_code
            redirect-uri: "http://127.0.0.1:8090/authorized"
            scope:
              - openid
              - profile
            client-name: ferr-jose-auth-server
        provider:
          spring:
            issuer-uri: http://localhost:9100

server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway

  endpoint:
    gateway:
      access: unrestricted